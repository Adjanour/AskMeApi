[tool.poetry]
name = "askmeapi"
version = "0.1.0"
description = "api"
authors = ["adjanour <adjanour@icloud.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
